#include "main.h"

/**
 * mul - add two given  numbers.
 *
 * @a: first number
 * @b: second number
 * Return: Always the product.
 */
int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
